Drop an applause sound effect here named exactly:

    applause.mp3

The overlay will play it when "Trigger applause" is pressed.
If this file is missing, the overlay falls back to a synthesized
applause sound automatically, so the button always works.

Good free sources: Pixabay, Freesound (check the license).
