applause.mp3 — the sound the overlay plays on "Applause".

A synthesized crowd-applause clip ships here by default (generated by
scripts/gen-applause.mjs). To use your own, just replace applause.mp3 with
any mp3 of the same name.

If the file is ever missing, the overlay falls back to a Web-Audio
synthesized applause so the button always works.
